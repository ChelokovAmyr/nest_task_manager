<<<<<<< HEAD
# Task Manager - Full-Stack Application

Полнофункциональное приложение для управления задачами с современным стеком технологий.

## 🚀 Технологии

### Backend
- **NestJS** - Прогрессивный Node.js фреймворк
- **TypeORM** - ORM для работы с базой данных
- **PostgreSQL** - Реляционная база данных
- **JWT** - Аутентификация и авторизация
- **Swagger** - API документация
- **Docker** - Контейнеризация базы данных

### Frontend
- **Nuxt 3** - Vue.js фреймворк
- **Vue 3** - Прогрессивный JavaScript фреймворк
- **TypeScript** - Типизированный JavaScript
- **Axios** - HTTP клиент
- **Modern UI** - Современный интерфейс с анимациями

## 📋 Функциональность

- ✅ Аутентификация и регистрация пользователей
- ✅ Управление задачами (создание, редактирование, удаление)
- ✅ Фильтрация задач по статусу и приоритету
- ✅ Поиск задач
- ✅ Сортировка задач
- ✅ Статистика задач
- ✅ Профиль пользователя
- ✅ Модальные окна для подтверждений и уведомлений
- ✅ Плавные анимации и переходы

## 🛠️ Установка и запуск

### Требования
- Node.js 18+
- Docker Desktop (для PostgreSQL)
- npm или yarn

### Быстрый старт

1. **Клонируйте репозиторий**
```bash
git clone <URL_РЕПОЗИТОРИЯ>
cd nest_training
```

2. **Запустите проект**
```bash
# Windows PowerShell
.\start-project.ps1

# Или вручную:
# 1. Запустите базу данных
cd task-manager-api
docker-compose up -d

# 2. Запустите бэкенд
cd task-manager-api
npm install
npm run start:dev

# 3. Запустите фронтенд (в новом терминале)
cd task-manager-frontend
npm install
npm run dev
```

3. **Заполните базу данных тестовыми данными**
```bash
cd task-manager-api
npm run seed
```

## 🔑 Учетные данные

После выполнения seed скрипта доступны следующие пользователи:

- **Администратор**: `admin@admin.com` / `Admin123`
- **Тестовый пользователь**: `test@test.com` / `Test123`
- **Демо пользователь**: `demo@demo.com` / `Demo123`

## 📡 API Endpoints

### Аутентификация
- `POST /api/v1/auth/register` - Регистрация
- `POST /api/v1/auth/login` - Вход

### Задачи
- `GET /api/v1/tasks` - Получить все задачи (с фильтрами)
- `GET /api/v1/tasks/:id` - Получить задачу по ID
- `POST /api/v1/tasks` - Создать задачу
- `PATCH /api/v1/tasks/:id` - Обновить задачу
- `DELETE /api/v1/tasks/:id` - Удалить задачу
- `GET /api/v1/tasks/stats` - Получить статистику

### Пользователи
- `GET /api/v1/users/profile` - Получить профиль
- `PATCH /api/v1/users/profile` - Обновить профиль

Полная документация API доступна по адресу: `http://localhost:3001/api/docs`

## 🗂️ Структура проекта

```
nest_training/
├── task-manager-api/          # Backend (NestJS)
│   ├── src/
│   │   ├── auth/              # Модуль аутентификации
│   │   ├── tasks/             # Модуль задач
│   │   ├── users/             # Модуль пользователей
│   │   ├── common/            # Общие компоненты
│   │   └── database/          # Seed скрипты
│   └── docker-compose.yml     # Конфигурация Docker
│
├── task-manager-frontend/      # Frontend (Nuxt 3)
│   ├── components/            # Vue компоненты
│   ├── composables/           # Композаблы
│   ├── pages/                # Страницы
│   ├── layouts/              # Макеты
│   └── plugins/              # Плагины
│
├── start-project.ps1          # Скрипт запуска (Windows)
└── stop-project.ps1          # Скрипт остановки (Windows)
```

## 🔧 Конфигурация

### Backend
Создайте файл `.env` в `task-manager-api/`:
```env
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_DATABASE=task_manager
PORT=3001
JWT_SECRET=your-secret-key
JWT_EXPIRATION=7d
```

### Frontend
Конфигурация в `task-manager-frontend/nuxt.config.ts`:
- API URL: `http://localhost:3001/api/v1`

## 🐳 Docker

База данных PostgreSQL запускается через Docker Compose:
```bash
cd task-manager-api
docker-compose up -d
```

## 📝 Скрипты

### Backend
- `npm run start:dev` - Запуск в режиме разработки
- `npm run build` - Сборка проекта
- `npm run start:prod` - Запуск в продакшене
- `npm run seed` - Заполнение базы данных

### Frontend
- `npm run dev` - Запуск в режиме разработки
- `npm run build` - Сборка проекта
- `npm run preview` - Просмотр production сборки

## 🎨 Особенности UI

- Плавные анимации и переходы
- Модальные окна для подтверждений
- Уведомления (toast notifications)
- Адаптивный дизайн
- Современный градиентный дизайн
- Плавная прокрутка без прыжков

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Создано с использованием NestJS и Nuxt 3
=======

>>>>>>> 34c9e6d155f2915424bc604b464ecbb43e8afb8f
