# Task Manager Frontend

Современный фронтенд для Task Manager API, построенный на Nuxt 3, Vue 3 и TypeScript.

## 🚀 Возможности

- ✅ **Аутентификация** - Регистрация и вход с JWT токенами
- ✅ **Управление задачами** - CRUD операции с красивым UI
- ✅ **Фильтрация и поиск** - По статусу, приоритету, текстовый поиск
- ✅ **Сортировка** - По различным полям
- ✅ **Статистика** - Красивые карточки со статистикой задач
- ✅ **Профиль пользователя** - Управление профилем
- ✅ **Адаптивный дизайн** - Работает на всех устройствах
- ✅ **Современный UI** - Красивый и интуитивный интерфейс

## 📋 Требования

- Node.js (v18 или выше)
- npm или yarn
- Запущенный Task Manager API (бэкенд)

## 🛠 Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ChelokovAmyr/nest_task_manager_frontend.git
cd nest_task_manager_frontend
```

2. Установите зависимости:
```bash
npm install
```

3. (Опционально) Создайте файл `.env`:
```env
API_BASE_URL=http://localhost:3000/api/v1
```

**⚠️ Важно:** Не коммитьте `.env` файл в репозиторий! Он уже добавлен в `.gitignore`.

4. Запустите приложение:
```bash
# Режим разработки
npm run dev

# Продакшн режим
npm run build
npm run preview
```

Приложение будет доступно по адресу: `http://localhost:3001` (или другой порт, если 3001 занят)

## 📚 Структура проекта

```
task-manager-frontend/
├── assets/          # Стили и ресурсы
│   └── css/
│       └── main.css # Глобальные стили
├── components/      # Vue компоненты
│   ├── TaskCard.vue
│   └── TaskModal.vue
├── composables/     # Composable функции
│   ├── useAuth.ts   # Аутентификация
│   └── useTasks.ts  # Работа с задачами
├── layouts/         # Layout компоненты
│   └── default.vue  # Основной layout
├── middleware/      # Middleware для роутов
│   └── auth.ts      # Защита роутов
├── pages/           # Страницы
│   ├── dashboard.vue
│   ├── login.vue
│   ├── profile.vue
│   └── register.vue
├── utils/           # Утилиты
│   └── api.ts       # API клиент
└── nuxt.config.ts   # Конфигурация Nuxt
```

## 🎨 Основные страницы

- **Login** (`/login`) - Страница входа
- **Register** (`/register`) - Страница регистрации
- **Dashboard** (`/dashboard`) - Главная страница с задачами
- **Profile** (`/profile`) - Профиль пользователя

## 🔧 Технологии

- **Nuxt 3** - Vue.js фреймворк
- **Vue 3** - Прогрессивный JavaScript фреймворк
- **TypeScript** - Типизированный JavaScript
- **Axios** - HTTP клиент
- **VueUse** - Коллекция утилит для Vue

## 📝 Использование

1. Запустите бэкенд (Task Manager API) на порту 3000
2. Запустите фронтенд: `npm run dev`
3. Откройте браузер и перейдите на `http://localhost:3001`
4. Зарегистрируйтесь или войдите в систему
5. Начните создавать и управлять задачами!

## 🔒 Безопасность

- JWT токены хранятся в localStorage
- Автоматический редирект при истечении токена
- Защищенные роуты через middleware
- Валидация форм на клиенте и сервере

## 🔗 Связанные проекты

- Backend: [NestJS Task Manager API](https://github.com/ChelokovAmyr/nest_task_manager)

## 📝 Лицензия

MIT
